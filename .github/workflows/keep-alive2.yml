name: Keep Render App Awake 2

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: |
          curl -s https://paylocity-auto-clock-48hm.onrender.com > /dev/null
          echo "Ping successful!"
